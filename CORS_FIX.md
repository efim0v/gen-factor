# ✅ CORS Проблема Решена!

## Что было исправлено

### Проблема
```
API request failed for /meta/databases?group=bmk: TypeError: Failed to fetch
```

Браузер блокировал прямые запросы к API из-за CORS (Cross-Origin Resource Sharing).

### Решение
Настроен **Vite proxy сервер**, который обходит CORS ограничения.

## Изменённые файлы

### 1. `vite.config.ts`
Добавлен прокси сервер:
```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://92.50.154.150:45501/blup',
      changeOrigin: true,
      configure: (proxy) => {
        proxy.on('proxyReq', (proxyReq) => {
          // Автоматически добавляет Basic Auth
          const auth = Buffer.from('blup_user:1AMoVtyg').toString('base64');
          proxyReq.setHeader('Authorization', `Basic ${auth}`);
        });
      },
    },
  },
}
```

### 2. `services/httpClient.ts`
Изменён базовый URL с удалённого на локальный:
```typescript
// Было:
const API_BASE_URL = 'http://92.50.154.150:45501/blup/api';

// Стало:
const API_BASE_URL = '/api'; // Использует Vite proxy
```

## Как это работает

```
Браузер → Vite Dev Server (localhost:3000) → Proxy → API (92.50.154.150:45501)
           ↓
         /api/meta/databases
           ↓
    Proxy добавляет Auth
           ↓
  http://92.50.154.150:45501/blup/api/meta/databases
```

## Запуск приложения

```bash
# 1. Установите зависимости (если ещё не установлены)
npm install

# 2. Запустите dev сервер
npm run dev

# 3. Откройте браузер
http://localhost:3000
```

## Проверка работы

После запуска откройте консоль браузера (F12) и проверьте, что:
- ✅ Нет ошибок CORS
- ✅ Загружаются базы данных
- ✅ Загружаются породы, признаки и факторы

## Тестирование через curl

```bash
# После запуска npm run dev
curl "http://localhost:3000/api/meta/databases?group=bmk"
```

Должен вернуть список баз данных:
```json
[
  {"id":1,"code":"bmk_all","name":"bmk_all","description":null},
  {"id":2,"code":"bmk_dd","name":"bmk_dd","description":null},
  {"id":3,"code":"bmk_ll","name":"bmk_ll","description":null},
  {"id":4,"code":"bmk_yy","name":"bmk_yy","description":null}
]
```

## Что дальше?

Приложение теперь полностью работает и загружает данные из реального API! 🎉

Для продакшена потребуется настроить аналогичный прокси на веб-сервере (nginx/apache).
