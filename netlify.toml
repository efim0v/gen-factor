# Netlify configuration

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA redirect - must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
